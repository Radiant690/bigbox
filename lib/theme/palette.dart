// ? Contains all the colors
import 'package:flutter/rendering.dart';

class Palette {
  // ! Color is a class which takes an parameter such as 0xff040B13, it acts as a parametized constructor and initiates the new object variable almostBlack.
  static const Color almostBlack = Color(0xff040B13);
}
